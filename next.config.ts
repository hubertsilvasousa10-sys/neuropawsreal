import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["images.unsplash.com", "via.placeholder.com"], // coloque aqui os domínios das imagens externas que você usa
  },
};

export default nextConfig;

